export enum MembershipType {
	MONTHLY = 'MONTHLY',
	QUARTERLY = 'QUARTERLY',
	YEARLY = 'YEARLY',
	LIFETIME = 'LIFETIME',
}

export enum MembershipStatus {
	ACTIVE = 'ACTIVE',
	EXPIRED = 'EXPIRED',
	SUSPENDED = 'SUSPENDED',
}

