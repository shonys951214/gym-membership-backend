# 1차피드백 구현 플랜

## 📋 개요

1차피드백 반영 작업을 가중치 계산식 제외하고 우선순위별로 진행합니다.
**가중치 계산식 관련 작업(Phase 3, Phase 5)은 프로젝트 끝까지 수정하면서 진행할 예정이므로 제외합니다.**

---

## ✅ 이미 완료된 작업

1. **유연성(FLEXIBILITY) 추가**
   - ✅ `Category` enum에 `FLEXIBILITY` 추가
   - ✅ `AbilitySnapshot` 엔티티에 `flexibilityScore` 필드 추가
   - ✅ SQL 마이그레이션 파일 생성 (`database/add_flexibility_score_column.sql`)
   - ✅ API 응답에 6개 영역 반영 (헥사곤, 히스토리, 비교)
   - ✅ 지표 이름 변경 (근력 → 하체 근력 등)
   - ✅ `forFront.md` 문서 업데이트

---

## 🎯 구현 우선순위 (가중치 계산식 제외)

### Phase 1: 기본 기능 엔티티 및 API (최우선) ⭐

**목표**: 프론트엔드에서 즉시 사용 가능한 기본 기능 구현

#### 1-1. 목표 관리 기능

**DB 작업**:
- [ ] `Member` 엔티티에 목표 필드 추가
  - `goal`: string (회원의 목표 한줄 요약)
  - `goalProgress`: number (진행률 0-100)
  - `goalTrainerComment`: string (트레이너 동기부여 코멘트)
  - `totalSessions`: number (총 수업 회차)
  - `completedSessions`: number (완료된 수업 회차)
- [ ] SQL 마이그레이션 파일 생성 (`database/add_member_goal_fields.sql`)

**API 작업**:
- [ ] 목표 조회 API (`GET /api/members/:id/goal`)
- [ ] 목표 수정 API (`PUT /api/members/:id/goal`)
- [ ] DTO 생성 (`UpdateGoalDto`)
- [ ] Service 로직 구현
- [ ] Controller 구현
- [ ] Swagger 문서화

**예상 작업 시간**: 2-3시간

---

#### 1-2. 운동 기록 기능

**DB 작업**:
- [ ] `WorkoutRecord` 엔티티 생성
  - `id`: uuid
  - `memberId`: uuid (FK)
  - `workoutDate`: date
  - `bodyPart`: string (부위: 하체, 가슴, 등, 어깨, 팔 등)
  - `exerciseName`: string (운동명)
  - `weight`: number (무게, kg)
  - `reps`: number (횟수)
  - `sets`: number (세트 수)
  - `volume`: number (볼륨 = weight * reps * sets, 자동 계산)
  - `workoutType`: enum ('PT' | 'PERSONAL')
  - `createdAt`, `updatedAt`
- [ ] SQL 마이그레이션 파일 생성 (`database/create_workout_records_table.sql`)

**API 작업**:
- [ ] 운동 기록 목록 조회 (`GET /api/members/:id/workout-records`)
- [ ] 운동 기록 생성 (`POST /api/members/:id/workout-records`)
- [ ] 운동 기록 수정 (`PUT /api/members/:id/workout-records/:recordId`)
- [ ] 운동 기록 삭제 (`DELETE /api/members/:id/workout-records/:recordId`)
- [ ] 부위별 볼륨 조회 (`GET /api/members/:id/workout-records/volume?period=week|month`)
- [ ] DTO 생성 (`CreateWorkoutRecordDto`, `UpdateWorkoutRecordDto`, `WorkoutVolumeQueryDto`)
- [ ] Service 로직 구현 (볼륨 자동 계산 포함)
- [ ] Controller 구현
- [ ] Swagger 문서화

**예상 작업 시간**: 4-5시간

---

#### 1-3. PT 세션 관리

**DB 작업**:
- [ ] `PTSession` 엔티티 생성
  - `id`: uuid
  - `memberId`: uuid (FK)
  - `sessionNumber`: number (수업 회차: 1, 2, 3...)
  - `sessionDate`: date
  - `mainContent`: string (주요 수업 내용)
  - `trainerComment`: string (트레이너 코멘트, nullable)
  - `createdAt`, `updatedAt`
- [ ] SQL 마이그레이션 파일 생성 (`database/create_pt_sessions_table.sql`)

**API 작업**:
- [ ] PT 세션 목록 조회 (`GET /api/members/:id/pt-sessions`)
- [ ] PT 세션 생성 (`POST /api/members/:id/pt-sessions`)
- [ ] PT 세션 수정 (`PUT /api/members/:id/pt-sessions/:sessionId`)
- [ ] PT 세션 삭제 (`DELETE /api/members/:id/pt-sessions/:sessionId`)
- [ ] DTO 생성 (`CreatePTSessionDto`, `UpdatePTSessionDto`)
- [ ] Service 로직 구현 (세션 번호 자동 계산)
- [ ] Controller 구현
- [ ] Swagger 문서화

**Member 엔티티 연동**:
- [ ] PT 세션 생성 시 `completedSessions` 자동 업데이트
- [ ] PT 세션 삭제 시 `completedSessions` 자동 감소

**예상 작업 시간**: 3-4시간

---

#### 1-4. 추천 운동 루틴

**DB 작업**:
- [ ] `WorkoutRoutine` 엔티티 생성
  - `id`: uuid
  - `memberId`: uuid (FK)
  - `routineDate`: date (오늘의 운동 루틴 날짜)
  - `exercises`: jsonb (운동 목록 배열)
    ```json
    [
      {
        "exerciseName": "스쿼트",
        "sets": 3,
        "reps": 10,
        "weight": 60,
        "rest": 60
      }
    ]
    ```
  - `isCompleted`: boolean (완료 여부)
  - `completedAt`: timestamp (완료 시간, nullable)
  - `createdAt`, `updatedAt`
- [ ] SQL 마이그레이션 파일 생성 (`database/create_workout_routines_table.sql`)

**API 작업**:
- [ ] 오늘의 운동 루틴 조회 (`GET /api/members/:id/workout-routines/today`)
- [ ] 운동 루틴 생성 (`POST /api/members/:id/workout-routines`)
- [ ] 운동 루틴 수정 (`PUT /api/members/:id/workout-routines/:routineId`)
- [ ] 운동 루틴 완료 처리 (`PATCH /api/members/:id/workout-routines/:routineId/complete`)
- [ ] 운동 루틴 삭제 (`DELETE /api/members/:id/workout-routines/:routineId`)
- [ ] DTO 생성 (`CreateWorkoutRoutineDto`, `UpdateWorkoutRoutineDto`)
- [ ] Service 로직 구현
- [ ] Controller 구현
- [ ] Swagger 문서화

**예상 작업 시간**: 3-4시간

---

### Phase 2: 대시보드 통합 ⭐

**목표**: 여러 데이터 소스를 통합한 대시보드 API 제공

#### 2-1. 대시보드 API 확장

**API 작업**:
- [ ] 대시보드 통합 API 생성 (`GET /api/members/:id/dashboard`)
- [ ] 응답 데이터 구조 설계:
  ```typescript
  {
    goal: {
      goal: string;
      goalProgress: number;
      goalTrainerComment: string;
    };
    sessionProgress: {
      totalSessions: number;
      completedSessions: number;
      progressPercentage: number;
    };
    workoutCalendar: {
      date: string;
      ptSessions: PTSession[];
      personalWorkouts: WorkoutRecord[];
    }[];
    workoutAnalysis: {
      period: 'week' | 'month';
      bodyPartVolumes: {
        bodyPart: string;
        volume: number;
      }[];
    };
  }
  ```
- [ ] Service 로직 구현 (여러 데이터 소스 통합)
- [ ] Controller 구현
- [ ] Swagger 문서화

**예상 작업 시간**: 3-4시간

---

### Phase 4: 평가 시스템 개선

**목표**: 정적/동적 평가 구분

#### 4-1. Assessment 엔티티 확장

**DB 작업**:
- [ ] `Assessment` 엔티티에 필드 추가
  - `evaluationType`: enum ('STATIC' | 'DYNAMIC')
  - `staticEvaluation`: jsonb (정적평가 데이터)
  - `dynamicEvaluation`: jsonb (동적평가 데이터)
- [ ] SQL 마이그레이션 파일 생성 (`database/add_evaluation_type_to_assessments.sql`)

**API 작업**:
- [ ] 평가 생성 시 `evaluationType` 필수 입력
- [ ] 정적평가/동적평가별 데이터 구조 설계
- [ ] DTO 수정 (`CreateAssessmentDto`, `UpdateAssessmentDto`)
- [ ] Service 로직 수정
- [ ] Swagger 문서화

**예상 작업 시간**: 2-3시간

---

## 📊 전체 작업 일정 예상

| Phase | 작업 내용 | 예상 시간 | 우선순위 |
|-------|----------|----------|----------|
| Phase 1-1 | 목표 관리 | 2-3시간 | 최우선 |
| Phase 1-2 | 운동 기록 | 4-5시간 | 최우선 |
| Phase 1-3 | PT 세션 관리 | 3-4시간 | 최우선 |
| Phase 1-4 | 추천 운동 루틴 | 3-4시간 | 최우선 |
| Phase 2 | 대시보드 통합 | 3-4시간 | 2순위 |
| Phase 4 | 평가 시스템 개선 | 2-3시간 | 4순위 |
| **합계** | | **17-23시간** | |

---

## 🚫 제외된 작업 (프로젝트 끝까지 수정하면서 진행)

### Phase 3: 점수 계산 로직 확장 설계
- ScoringCriteria 엔티티 생성
- AssessmentWeightConfig 엔티티 생성
- AssessmentGradeConfig 엔티티 생성
- 점수 계산 로직을 DB 조회 방식으로 변경
- 체중 대비 비율 계산 로직 구현

### Phase 5: 가중치/등급 커스터마이징
- 가중치 설정 API
- 등급 기준 설정 API
- 평가 기준표 관리 API
- 커스터마이징 UI

---

## 📝 작업 순서 권장사항

### 1단계: 목표 관리 (가장 간단, 빠른 완성)
- Member 엔티티만 수정하면 되므로 가장 빠르게 완성 가능
- 프론트엔드에서 즉시 사용 가능

### 2단계: PT 세션 관리
- 목표 관리와 연동 (completedSessions 업데이트)
- 세션 번호 자동 계산 로직 필요

### 3단계: 운동 기록
- 볼륨 자동 계산 로직 필요
- 부위별 집계 로직 필요

### 4단계: 추천 운동 루틴
- JSONB 데이터 구조 설계 필요
- 완료 처리 로직 필요

### 5단계: 대시보드 통합
- 위 4개 기능이 완성된 후 진행
- 여러 데이터 소스 통합 로직 필요

### 6단계: 평가 시스템 개선
- 정적/동적 평가 구분
- 기존 평가 시스템과의 호환성 고려 필요

---

## 🔍 각 Phase별 상세 체크리스트

### Phase 1-1: 목표 관리 체크리스트

**엔티티**:
- [ ] `src/entities/member.entity.ts`에 필드 추가
- [ ] TypeORM 데코레이터 설정

**SQL**:
- [ ] `database/add_member_goal_fields.sql` 생성
- [ ] 컬럼 추가 SQL 작성
- [ ] 기존 데이터 마이그레이션 고려

**DTO**:
- [ ] `src/modules/members/dto/update-goal.dto.ts` 생성
- [ ] Validation 데코레이터 추가

**Service**:
- [ ] `src/modules/members/members.service.ts`에 메서드 추가
- [ ] 목표 조회 로직
- [ ] 목표 수정 로직

**Controller**:
- [ ] `src/modules/members/members.controller.ts`에 엔드포인트 추가
- [ ] Swagger 데코레이터 추가
- [ ] 권한 설정 (ADMIN, TRAINER)

**테스트**:
- [ ] API 테스트 (Swagger 또는 Postman)
- [ ] 에러 케이스 테스트

---

### Phase 1-2: 운동 기록 체크리스트

**엔티티**:
- [ ] `src/entities/workout-record.entity.ts` 생성
- [ ] Member와 관계 설정
- [ ] 인덱스 설정 (memberId, workoutDate)

**SQL**:
- [ ] `database/create_workout_records_table.sql` 생성
- [ ] 테이블 생성 SQL 작성
- [ ] 외래키 설정

**DTO**:
- [ ] `src/modules/members/dto/create-workout-record.dto.ts` 생성
- [ ] `src/modules/members/dto/update-workout-record.dto.ts` 생성
- [ ] `src/modules/members/dto/workout-volume-query.dto.ts` 생성

**Service**:
- [ ] `src/modules/members/workout-records.service.ts` 생성
- [ ] CRUD 로직 구현
- [ ] 볼륨 자동 계산 로직
- [ ] 부위별 집계 로직

**Controller**:
- [ ] `src/modules/members/workout-records.controller.ts` 생성
- [ ] 엔드포인트 구현
- [ ] Swagger 문서화

**Module**:
- [ ] `src/modules/members/members.module.ts`에 WorkoutRecordsService 추가

---

### Phase 1-3: PT 세션 관리 체크리스트

**엔티티**:
- [ ] `src/entities/pt-session.entity.ts` 생성
- [ ] Member와 관계 설정
- [ ] 인덱스 설정 (memberId, sessionDate)

**SQL**:
- [ ] `database/create_pt_sessions_table.sql` 생성

**DTO**:
- [ ] `src/modules/members/dto/create-pt-session.dto.ts` 생성
- [ ] `src/modules/members/dto/update-pt-session.dto.ts` 생성

**Service**:
- [ ] `src/modules/members/pt-sessions.service.ts` 생성
- [ ] 세션 번호 자동 계산 로직
- [ ] Member의 completedSessions 업데이트 로직

**Controller**:
- [ ] `src/modules/members/pt-sessions.controller.ts` 생성

---

### Phase 1-4: 추천 운동 루틴 체크리스트

**엔티티**:
- [ ] `src/entities/workout-routine.entity.ts` 생성
- [ ] JSONB 필드 설정

**SQL**:
- [ ] `database/create_workout_routines_table.sql` 생성

**DTO**:
- [ ] `src/modules/members/dto/create-workout-routine.dto.ts` 생성
- [ ] `src/modules/members/dto/update-workout-routine.dto.ts` 생성

**Service**:
- [ ] `src/modules/members/workout-routines.service.ts` 생성
- [ ] 오늘의 루틴 조회 로직
- [ ] 완료 처리 로직

**Controller**:
- [ ] `src/modules/members/workout-routines.controller.ts` 생성

---

### Phase 2: 대시보드 통합 체크리스트

**Service**:
- [ ] `src/modules/members/dashboard.service.ts` 생성
- [ ] 여러 데이터 소스 통합 로직
- [ ] 캘린더 데이터 집계 로직

**Controller**:
- [ ] `src/modules/members/dashboard.controller.ts` 생성
- [ ] 통합 API 엔드포인트 구현

**DTO**:
- [ ] `src/modules/members/dto/dashboard-response.dto.ts` 생성

---

### Phase 4: 평가 시스템 개선 체크리스트

**엔티티**:
- [ ] `src/entities/assessment.entity.ts`에 필드 추가
- [ ] EvaluationType enum 생성

**SQL**:
- [ ] `database/add_evaluation_type_to_assessments.sql` 생성

**DTO**:
- [ ] `CreateAssessmentDto` 수정
- [ ] `UpdateAssessmentDto` 수정

**Service**:
- [ ] 평가 생성/수정 로직 수정
- [ ] 정적/동적 평가 구분 로직

---

## 📌 주의사항

1. **데이터 마이그레이션**: 기존 데이터가 있는 경우 마이그레이션 전략 수립 필요
2. **API 호환성**: 기존 API와의 호환성 유지
3. **권한 설정**: 모든 API에 적절한 권한 설정 (ADMIN, TRAINER)
4. **에러 처리**: 일관된 에러 처리 및 응답 형식 유지
5. **Swagger 문서화**: 모든 API에 Swagger 데코레이터 추가
6. **테스트**: 각 기능 완성 후 API 테스트 필수

---

## 🎯 다음 단계

1. **Phase 1-1 (목표 관리)**부터 시작
2. 각 Phase 완성 후 테스트 및 문서화
3. 프론트엔드와 협업하여 API 사용 확인
4. 피드백 반영 후 다음 Phase 진행

---

**작성일**: 2026-01-06
**상태**: 구현 대기 중

